/**
 * Audio Single Player constants.
 */

export const defaults = {
  INITIAL_PROGRESS: 0,
  PLAYHEAD_OFFSET: -9,
  TICKER_INCREMENT: 250,
  STEP_INCREMENT: 5,
}

export const constants = {
  FORWARD: true,
  BACKWARD: false,
}
